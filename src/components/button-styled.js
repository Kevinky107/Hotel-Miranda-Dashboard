import { styled } from 'styled-components';

export const GreenButtonDark = styled.button`
  background: #135846;
  border-radius: 0.5em;
  border: none;
  color: white;
  padding: 0.8em;
  padding-inline: 4em;
  &:hover{
    cursor: pointer
  }
`
export const GreenButtonLight = styled.button`
  background: ##EBF1EF;
  border-radius: 0.5em;
  border: none;
  color: #135846;
  padding: 0.8em;
  padding-inline: 4em;
  &:hover{
    cursor: pointer
  }
`
